# AI Memo — Генератор Сказок

Данный файл содержит важные заметки об архитектурных решениях, багах и «подводных камнях» проекта, предназначенные для будущих сессий разработки.

---

## 19.03.2026 (Архитектурные ограничения Streamlit & HTML в st.button)
- **Урок**: Попытка передать сырой HTML в параметр `label` у `st.button` (с целью сделать сложный UI-блок кликабельным) всегда приводит к ошибке — Streamlit экранирует HTML и выводит его сырым текстом.
- **Правильный подход**: Для создания сложных оформленных карточек следует использовать `st.container(border=True)` с разделением на колонки `st.columns`. Стилизовать форму карточки можно глобально через CSS (таргетинг `div[data-testid="stVerticalBlockBorderWrapper"]`), но элементы интерфейса должны оставаться нативными дочерними компонентами Streamlit.

## Регламент обновления версии (v3.4+)
- **Источник Истины**: Единственным местом определения версии является `config.py` (`APP_VERSION`).
- **Автоматизация для ИИ**: При выполнении задач по обновлению документации или деплою (GitHub), Агент **ОБЯЗАН** самостоятельно инкрементировать версию в `config.py`, если были внесены функциональные изменения.
- **Синхронизация**: Номер версии в `config.py`, `README.md` и `ARCHITECTURE.md` должен быть идентичным.
- **Отображение**: В `app.py` (сайдбар) и `landing.py` версия подтягивается из конфига. Хардкод номеров в UI запрещен.

## 18.03.2026 (Сессия 7: Откат v4.7 — OpenRouter Fail)
- **Причина**: Интеграция OpenRouter (v4.7) вызвала критические ошибки кодировки (`charmap`, `UTF-8` garbage). Модели Stepfun и другие бесплатные провайдеры через OpenRouter выдавали нечитаемый текст в Streamlit.
- **Действие**: Полный `git reset --hard` и `git clean -f` до **v4.6**.
- **Статус**: Проект возвращен к стабильной работе на Google Gemini. Будущие попытки внедрения OpenRouter требуют проверки кодировки ответов (рекомендуется `response.text.encode('utf-8').decode('utf-8')` или аналоги).

## 16.03.2026 (Сессия 6: Полировка UI)
- **Улучшение UX селектора**: Заголовок изменен на "Выберите профиль ребенка" во всех языках.
- **Стабильность селектора**: `None` в состоянии заменен на `"none"`, добавлен явный `index=0`. Это решило проблему появления дефолтного плейсхолдера Streamlit "Choose an option".
- **Версия**: Глобальное обновление до **v4.1**.

## 16.03.2026 (Сессия 5: Интернационализация и исправление профилей)
- **Баг подстановки возраста**: В `app.py` исправлен рассинхрон типов. Возраст в БД (RU-ключ) теперь корректно мапится на переведенную метку UI через `t(f"age_ranges.{c_age}", user_lang)`.
- **Исправление i18n**: Функция `get_age_ranges()` в `i18n.py` была переписана для возврата переведенных меток в качестве ключей словаря. Это исправило баг, при котором в нерусскоязычных интерфейсах отображались русские возрастные категории.
- **Версия**: Обновление до **v4.0**.

## 12.03.2026 (Сессия 4: Визуальная атмосфера)
- **Лендинг**: Глобальное внедрение визуальных эффектов (звезды, иконки). Каждая секция теперь имеет динамический декоративный фон.
- **Анимации**: Добавлены 3 группы анимаций для иконок (`floatIcon1-3`).
- **Рефакторинг**: Локализация лендинга (`t()`) и критические импорты перенесены в начало `landing.py`. Улучшена производительность генерации декоративного HTML.
- **Версия**: Глобальное обновление до **v3.6**.

## 12.03.2026 (Сессия 3: Аналитика и Freemium)
- **Модель монетизации**: Полный отказ от 14-дневного триала Pro. Переход на модель **Freemium** (3 Free генерации навсегда).
- **Цены**: Актуализированы тарифы — **699₽ (Pro)** и **1199₽ (Family)**. Синхронизировано в `ARCHITECTURE.md` и `landing.py`.
- **Roadmap**: Добавлены планы по "Библиотеке классики" (Public Domain) и "Админ-панели" в Фазу 6.
- **Чистка**: Удалена переменная `pricing_subdesc` и соответствующий HTML-блок на лендинге.

---

## 04.03.2026 (Сессия 2: Актуализация версии)
- `config.py`: `APP_VERSION` обновлен с `v3.2` до **`v3.4`**.
- Проверено использование `APP_VERSION` в `app.py`.
- Добавлено правило о самоконтроле версии для ИИ.

---

## 04.03.2026 (Сессия 1: Рефакторинг и очистка)

**Что сделано за сессию:**
- `profile_page.py` — Личный кабинет с полной локализацией на 8 языков. Слово подтверждения удаления переводится (УДАЛИТЬ / DELETE / ELIMINAR / SUPPRIMER / LÖSCHEN / EXCLUIR / 删除 / हटाएं).
- **Выход → Лендинг**: `sign_out()` + `st.session_state.current_page = 'landing'` + `st.rerun()`.
- **Сайдбар**: кнопка «Профиль» — `type="tertiary"` (ghost), кнопка «Выйти» перенесена в самый низ панели.
- **Hero-сниппеты лендинга**: заголовки вшиты через Python (мгновенно), убран JS prefill-шаг. Фиксированная высота `7.5em` — карточки не прыгают.
- **Очистка файловой системы**: удалены 9 временных файлов/папок, дополнен `.gitignore`.

**Следующий шаг**: Фаза 4 — Supabase таблицы plans/subscriptions, middleware лимитов, ЮKassa/Paddle.

---

## Критически важные архитектурные решения

### Роутинг
Весь роутинг — через `st.session_state.current_page`. Доступные значения: `landing`, `generator`, `profile`, `privacy`, `terms`. Смена страницы всегда + `st.rerun()`.

### После выхода
```python
sign_out()
st.session_state.current_page = 'landing'
st.rerun()
```
Если не установить `current_page`, пользователь остаётся на генераторе (неаутентифицированный) вместо лендинга.

### Hero-сниппеты лендинга (landing.py)
- Заголовки и badges — Python f-string (мгновенный рендер, никакого JS ожидания).
- Текст — JS typing-эффект (начинается сразу при появлении DOM-элементов).
- Высота карточек фиксирована в CSS: `height: 7.5em; overflow: hidden;`.
- JS prefill-шаг **удалён** — он вызывал flash полного текста перед анимацией.

### Авторизация (auth.py)
- SessionBounded PKCE Flow — каждый браузер получает уникальный `client_id` через Cookie.
- IsolatedDiskStorage — сессии хранятся в `.auth_sessions/` (не в git, в .gitignore).
- Graceful fallback — если Supabase недоступен, приложение работает в гостевом режиме.

### Локализация профиля (profile_page.py)
- Словарь `L` содержит все 8 языков: ru, en, es, fr, de, pt, zh-CN, hi.
- `loc()` — фолбэк: `lang → en → ru` через `or` (не вложенные `.get()`).
- Ключ `del_confirm_word` — слово подтверждения удаления переводится для каждого языка.

---

## 28.02.2026
### Текущий статус: Мобильная адаптация завершена
- **UI/UX**: Исправлены критические ошибки мобильной версии (языковой селектор, размеры карточек, свайпы в отзывах).
- **Следующий шаг**: Настройка Supabase (аутентификация, таблица детских профилей, облачное сохранение историй, миграция с временного локального `stories.json`).
- **Примечание**: Проект полностью готов к интеграции облачной базы данных. Лендинг отполирован и адаптивен.

---

## 16.02.2026
### UI: Radio buttons и иконки в теме «День»
- **Radio buttons**: Имеют проблемы с видимостью в разных браузерах. `accent-color` работает не во всех браузерах одинаково.
- **Решение**: Глубокий CSS-рефакторинг (стили «пилюль» привязаны к `.st-key-*`). Замена виджета не требуется.
- **Иконки сказок**: В теме «День» были нечитаемыми — исправлено добавлением фиолетовой цветовой схемы.

### Локализация: STORY_PROMPTS
- **TRANSLATIONS**: Полные переводы UI для 8 языков.
- **STORY_PROMPTS**: Промпты на всех 8 языках — каждый язык генерирует сказку нативно.

---

## 13.02.2026
### Текущий статус: Личная библиотека и Полировка UI
- **Личная библиотека**: Реализована на гибридном хранилище (Supabase + `stories.json`). `storage.py` инкапсулирует всю логику.
- **Prompt Engineering**: Склонение имён в системном промпте значительно улучшило «натуральность». Сохранять этот блок при рефакторинге!
- **Критический фикс**: Удалены лишние бэктики в конце `app.py`. При массовом редактировании через LLM проверять чистоту закрывающих тегов.

### Технический долг
- [ ] **Аудио в JSON**: `BytesIO` не сериализуется в `stories.json`. Сказки в библиотеке без привязанного аудио. В Supabase — хранить URL на mp3 в Storage.
- [ ] **Профили детей**: Реализовано в v3.9 (CRUD + быстрая генерация).
- [ ] **Временная история (TTL)**: Продумать механизм удаления временных сказок через 72 часа. Возможные варианты: Supabase `expires_at` (TTL) или фильтрация на клиенте по `created_at`.

---
### 🔄 Миграция на google-genai SDK (10.03.2026)
Проведена миграция с устаревшего `google-generativeai` на современный `google-genai` SDK.  
**Ключевые изменения для будущих агентов**:
1. Импорт: `from google import genai`.
2. Клиент: `client = genai.Client(api_key=api_key)`.
3. Генерация: `client.models.generate_content(model=model_name, contents=prompt)`.  
**Внимание**: новый SDK автоматически загружает API-ключ из переменной окружения `GOOGLE_GENAI_API_KEY`, если он не передан в конструктор. В проекте ключ передается явно из `st.secrets`.
