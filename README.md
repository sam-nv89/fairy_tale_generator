# 🧚 Генератор Аудио-Сказок (MVP)

Умный веб-сервис для родителей, который генерирует персонализированные сказки с помощью ИИ и озвучивает их нейронными голосами.

## 🛠 Технологический стек
- **Python 3.10+**
- **Streamlit**: Web UI framework.
- **Google Gemini API**: Генерация текста сказок (бесплатный тариф Flash).
- **Edge-TTS**: Высококачественная нейронная озвучка (Microsoft Edge Voices).

## 📂 Структура проекта
- `app.py`: Основной файл приложения (Frontend + Backend логика).
- `requirements.txt`: Зависимости (`streamlit`, `google-generativeai`, `edge-tts`).
- `.streamlit/secrets.toml`: Файл конфигурации для безопасного хранения API ключа.

## 🚀 Как запустить (Инструкция)

### Шаг 1: Подготовка
Убедитесь, что у вас установлен Python.
Клонируйте репозиторий или откройте папку проекта.

### Шаг 2: Установка зависимостей
```bash
# Создаем виртуальное окружение (рекомендуется)
python -m venv venv

# Активируем
# Windows:
.\venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# Устанавливаем библиотеки
pip install -r requirements.txt
```

### Шаг 3: Настройка API Ключа
Для работы требуется ключ от [Google Gemini API](https://aistudio.google.com/app/apikey).
1. Создайте папку `.streamlit` в корне проекта (если нет).
2. Создайте файл `.streamlit/secrets.toml`.
3. Добавьте туда строку:
   ```toml
   GOOGLE_API_KEY = "ваш_ключ_здесь"
   ```
*Если этого не сделать, приложение запросит ключ через интерфейс.*

### Шаг 4: Запуск
```bash
streamlit run app.py
```
Сайт откроется по адресу: `http://localhost:8501`

### ✨ Возможности
*   **Генерация:** Уникальные истории по имени, возрасту и хобби ребенка.
*   **Озвучка:** Выбор мужского (Дмитрий) или женского (Светлана) голоса.
*   **Плеер:** Современный удобный плеер с перемоткой.
*   **Умные лимиты:** Обработка ограничений API и защита от сбоев.
